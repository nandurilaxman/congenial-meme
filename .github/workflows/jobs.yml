name: Jobs and Runners Demo
on: [push]
jobs:
  ubuntu-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running on Ubuntu"
  windows-job:
    runs-on: windows-latest
    steps:
      - run: echo "Running on Windows"
